import { PseudoAlgoIDE } from '@/components/pseudo-algo-ide';

/**
 * Exemple de page Next.js (App Router) exposant l'IDE à l'URL /pseudo-algo.
 * Adaptez le chemin à votre projet si besoin.
 */
export default function PseudoAlgoPage() {
  return <PseudoAlgoIDE />;
}
